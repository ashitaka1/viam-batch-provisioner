#!/usr/bin/env bash
# Fetched and run by autoinstall late-commands.
# Currently a placeholder — complex post-install logic can move here
# if the inline late-commands in user-data become unwieldy.
set -euo pipefail
echo "post-install.sh: nothing additional to do"
